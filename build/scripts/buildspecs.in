#!/bin/bash

PACKAGE=Krokit
ELFNAME=krokit
RELEASE=1.0
REVISION=a
VERSION=${RELEASE}-${REVISION}
HOMEPAGE=https://github.com/atomixcloud/krokit
MAINTAINER="Kirubakaran Shanmugam <kribakarans@gmail.com>"
DESCRIPTION="Krokit: Tool to setup OpenGrok source code search engine"

NC=$(tput sgr0)
BOLD=$(tput bold)

DISTDIR=dist
BUILDDIR=build
KTAGDIR=__ktags
TARGET_PKGSPECS=debian
SOURCE_PKGSPECS=debian-specs
PKGBUILDDIR=$BUILDDIR/$ELFNAME-build
PKGREPODIR=$PKGBUILDDIR/$ELFNAME-$RELEASE

#EOF
